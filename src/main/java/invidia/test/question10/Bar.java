package invidia.test.question10;

// Question: To ensure that some function foo() of class Bar, will be implemented seperately in every subclass and it should be declared as:

// Choices:
// abstract foo() .. Selected this !!
// public synchronized foo()
// protected abstract foo()
// protected foo()

public class Bar {

}
